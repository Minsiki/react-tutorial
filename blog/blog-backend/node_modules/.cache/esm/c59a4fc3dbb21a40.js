let Post;_dbb‍.x([["write",()=>write],["list",()=>list],["read",()=>read],["remove",()=>remove],["replace",()=>replace],["update",()=>update]]);_dbb‍.w("../../models/post",[["default",["Post"],function(v){Post=v}]]);

       const write = (ctx) => {};

       const list = (ctx) => {};

       const read = (ctx) => {};

       const remove = (ctx) => {};

       const replace = (ctx) => {};

       const update = (ctx) => {};

function notExistPost(ctx) {
  ctx.status = 404;
  ctx.message = {
    message: 'Not exist post',
  };

  return ctx;
}
